# 설계 원칙

## 목적

이 프로젝트는 한국 주식 실전 자동매매 시스템입니다.
아래 역할이 서로 섞이지 않도록 유지하는 것이 핵심입니다.

- 시장/증권사 입출력
- 매매 판단
- 리스크 통제
- 주문/보유 상태 관리
- 운영 보조 기능

## 반드시 지킬 원칙

1. 전략은 신호만 판단하고 실행은 하지 않는다.
2. 브로커는 키움 API 연결만 담당한다.
3. 엔진은 조정자 역할을 하되 전략 자체가 되면 안 된다.
4. 포트폴리오는 보유 상태와 실현손익의 기준점이다.
5. 리스크 규칙은 주문 전에 반드시 검사한다.
6. 운영 기능은 매매 로직과 분리한다.
7. 오버나이트 보유는 명시적인 전략 전제다.

## 기능 추가 전 확인 절차

1. 이 기능은 매매 로직인가, 리스크 로직인가, 상태 관리인가, 브로커 I/O인가, 운영 기능인가?
2. 지금 구조에서 이 책임은 어느 파일이 맡아야 하는가?
3. 어느 파일에는 절대 넣으면 안 되는가?
4. 기능 추가 전에 모듈 분리가 먼저 필요한가?
