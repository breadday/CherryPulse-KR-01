# D03 가상 손절 보호 진행 기록

- 기준: `main`의 D01 계약과 `docs/NEXT-DEVELOPMENT-PLAN.md`
- 상태: **부분 구현**. 순수 판단기, 비활성 규칙 저장, 매수 부분체결의 규칙 버전 연결 및 재시작 읽기를 검증했다. 청산 의무 생성과 가상 dispatcher 연결은 미완료다.

## 이번 범위

`contracts/stop_evaluation.py`는 확인된 보유 수량, 손절 관측, 기존 매도 예약, 미확정 매도 및 재대조 상태에서 **추가 보호 후보 수량**을 계산한다. 부분 매수 체결 4주에는 4주 후보, 4주 매도 예약 뒤에는 0주, 추가 매수 체결로 7주가 되면 3주 후보를 반환한다. 평가 당시 보유 수량과 원장 보유 수량이 다르면 차단한다. 매도 결과가 `UNKNOWN`이거나 재시작 후 대조가 필요하면 추가 후보는 0주로 차단한다. 이 결과는 주문 의도가 아니며 주문 전송 권한도 아니다.

사용자 결정: 손절 시세는 **최근 체결가**, 평균 매수가는 **확인된 매수 체결가의 수량 가중평균(수수료 제외)**, 첫 가상 손절 주문은 **시장가 우선**이다. 시세 모델은 최근 체결가만 받으며, 평균 계산은 비유한 가격과 빈 체결 목록을 거절한다. `execution.facts.Fill`에 선택적 정확한 체결 가격을 추가했다. 기존 가격 없는 사실은 그대로 읽되, 가격이 하나라도 없는 매수 주문의 평균은 알 수 없음(`None`)으로 남긴다. 여러 주문의 가격이 모두 확인되고 동일한 보호 규칙 버전이 붙어 있으며 기존 보유 수량·매도 체결·수량 불일치가 없을 때만 `confirmed_stop_cost_basis`가 보유분 가중평균을 계산한다. 매도 체결 후의 평균과 이전 보유의 원가는 아직 근거가 없어 `None`으로 남긴다. 시장가 손절 주문도 아직 생성하지 않는다.

## 검증

`Ledger.apply_virtual_stop_config`는 기존 가상 설정 버전을 확인하고 규칙과 새 버전을 하나의 SQLite 트랜잭션에 기록한다. `virtual_stop_binding`은 현재 버전과 일치하는 규칙만 읽는다. 기존 숫자 설정만 있는 버전은 규칙이 없는 것으로 처리한다. 저장만으로 감시나 주문이 시작되지 않으며 외부 설정 명령의 인증·승인 경로는 연결하지 않았다. 기존 가상 설정 기준 버전은 1이다.

확인된 가상 매수 `Fill`이 원장에 반영될 때, 그 주문의 `config_version`에 해당하는 규칙이 있으면 같은 SQLite 트랜잭션에 `StopRuleAssignment`를 기록한다. 키는 체결 이벤트 ID이며 체결 수량과 규칙 버전을 보존한다. 중복 체결은 원장 멱등 처리로 재기록되지 않는다. 나중에 현재 설정 버전이 바뀌어도 이미 접수된 매수 주문의 체결은 주문 당시 규칙 버전으로 연결한다. 규칙이 없던 기존 주문의 체결은 임의의 규칙을 붙이지 않는다. 이 기록은 청산 주문 의무나 감시 활성화가 아니다. 평균 매수가 비율 손절은 체결 가격 근거가 여전히 없어 실제 평가·실행을 차단한다.

추가 원장 검사: Linux 임시 테스트 fixture에서 Windows 전용 `msvcrt` import를 대체하고 lease를 수동 활성화해 SQLite 재시작 복구·버전 불일치 롤백·잘못된 규칙 거절, 부분체결과 중복 체결의 규칙 버전 보존, 매수 주문별·매도 전 보유분 가중평균 및 가격 없는 체결·기존 보유·매도 후 계산 차단을 검증했다. **6 passed**. 이 fixture는 Windows 계좌 잠금 동작을 검증하지 않는다.

Linux에서 Windows 전용 공통 `tests/conftest.py`를 사용하지 않도록 관련 테스트 두 파일을 임시 디렉터리에서 실행했다. `PYTHONPATH=/tmp/cherrypulse-test-deps:. python -m pytest -q /tmp/cherrypulse-contract-check`: **18 passed**. `ruff check contracts tests/test_settings_contract.py tests/test_stop_evaluation.py`: **통과**. `git diff --check`: **통과**. 임시 도구 경로와 복사한 테스트 디렉터리는 이 작업 환경에만 해당한다.

## 아직 필요한 연결과 결정

1. 원하는 설정 명령의 인증·수락과 로컬 가상 설정 적용을 연결하고, 보유분의 규칙 버전을 체결 시점에 고정하여 원장에 기록한다. 현재 원하는 설정의 `ACCEPTED`는 감시 시작을 뜻하지 않는다.
2. 기존 보유의 원가 인계, 매도 후 잔여 원가, 규칙 버전 변경 중 보유분의 정책과 근거를 확정한다. 확인 가능한 매수 전용 보유분의 비용 근거를 손절 조건 평가와 연결하고, 같은 틱·이벤트 재처리 시 청산 의무가 중복 생성되지 않도록 한다. 기존 가상 청산 스위치와 신규 손절 의무의 관계도 명시한다.
3. `UNKNOWN` 해소는 증권사 조회 근거에 따르며, 재시작 직후 보유·주문 대조 전 자동 매도를 차단한다. 실제 키움 주문 API는 연결하지 않는다.
4. 최근 체결가의 최대 허용 지연, 거래 세션, 시장가가 거절되거나 체결되지 않을 때의 행동을 정해야 감시와 주문을 활성화할 수 있다.

Windows 3.10 32비트 환경의 전체 회귀 검사와 키움 실증은 수행하지 않았다. 운영 DB 변경과 배포도 수행하지 않았다.
