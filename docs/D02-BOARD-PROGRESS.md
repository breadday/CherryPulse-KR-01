# D02 관심종목·손절 패턴 보드 진행 기록

## JSON/CSV 종목 초안 가져오기 — 2026-09-25

관심 종목 패널에서 UTF-8 JSON 또는 CSV 파일을 브라우저 로컬 초안으로
가져온다. JSON은 `{code,name,source?,note?}` 객체 배열, CSV는 `code,name`
필수 헤더와 선택형 `source,note` 헤더를 받으며 열 순서, 따옴표·쉼표·줄바꿈을
지원한다. 허용 필드 외 값, 잘못된 종목코드·이름·링크·메모, 파일 내/기존
초안과의 중복 코드, 5 MiB 초과 파일은 거절한다. 모든 행을 먼저 검증하므로
하나라도 오류면 일부 종목을 추가하지 않는다. 가져온 종목은 패턴 미연결·미보관
상태이며 감시나 주문을 시작하지 않는다.

검증: Node 단위/DOM 모의 검사에서 CSV 따옴표·줄바꿈, JSON/CSV 정규화, 필드·행
오류, 중복 시 무변경을 확인했다. Chrome 모바일 emulation에서 가상 CSV 파일을
제출해 종목 2개가 모두 카드로 표시되고 저장 안내가 나오는 것까지 확인했다.
5 MiB 한도 초과 시 파일 읽기 전 거부하는 테스트도 통과했다. 실제 사용자 파일과
물리 모바일은 미검증이다.

## 관심종목 JSON 내보내기 — 2026-09-25

활성 관심종목이 있을 때 코드·이름·링크·메모를 가져오기와 호환되는 JSON 배열로
다운로드한다. 보관 종목과 손절 패턴·종목별 패턴 연결은 내보내지 않으며, 따라서
전체 보드 백업이나 엔진 설정 내보내기가 아니다. 저장소 상태를 바꾸지 않고,
다운로드는 사용자가 버튼을 눌렀을 때만 시작한다.

검증: Node 검사에서 JSON 내용·필드 선택·보관 항목 제외·Blob URL 해제를 확인했다.
Chrome headless에서 버튼을 실제 클릭해 JSON 다운로드 완료 이벤트를 받았고,
내려받은 파일 내용이 활성 종목만 포함하는 것을 읽어 검증했다. 브라우저 에뮬레이션
외의 OS별 다운로드 동작은 미검증이다.

## 불변 버전 초안 추가 — 2026-09-25

보드에서 저장한 손절 패턴을 기존 값에 덮어쓰지 않고 새 버전으로 만들 수
있도록 했다. 신규 패턴은 v1이며, 기존 패턴 카드의 `새 패턴 버전 만들기`는
이름·발동 기준·기준값·시장가/지정가 선택을 복사해 같은 패턴 계열의 다음
버전으로 저장한다. 주문 방식 선택은 로컬 초안일 뿐 실행 설정은 아니다.
지정가 가격, 호가, 거래 세션과 미체결 처리는 미정으로 남긴다.
이전 버전에 연결된 종목은 그대로 유지되고, 새 버전은 종목 카드에서 직접
다시 연결해야 한다. 버전 비활성화는 이후 새 연결만 막고 기존 연결과
초안 이력을 유지한다. 이미 연결된 비활성 버전은 종목 카드에 표시되며,
새 연결 대상으로는 선택할 수 없다. 재활성화하면 다시 선택할 수 있다.
구형 로컬 초안은 `version`이 없으면 v1, `patternId`가 없으면 기존 ID를
계열 ID로 취급하고 `active`가 없으면 활성 버전으로 읽는다.

검증: `node --test web/board/board.test.js` **8 passed**. 신규 버전 저장 뒤
기존 버전과 종목 연결이 보존되는 것, 비활성 버전의 새 연결 차단 및 재활성화를,
새 버전에 시장가/지정가 선택이 저장되는 것을 검사했다. `node --check
web/board/board.js`, `node --check web/board/board.test.js`, `git diff --check`
통과. Node 검사는 DOM 모의 환경이며 실제 Chrome 결과는 위의 JSON/CSV 진행 기록을
참조한다. 별도 회귀 확인으로
Python 3.10.8 32비트 전체 pytest **159 passed**, Ruff 검사·포맷 검사,
가상 데모, pip 의존성 검사가 통과했다. Python은 격리된 `.venv`가 아닌
사용자 설치 환경에서 실행했다. 로컬 초안의 버전 표시는 엔진의 불변 패턴
저장이나 적용 상태가 아니며 서버 저장·엔진 동기화는 미구현이다.

## 종목 기본 정보 편집

종목 카드에서 종목명·참고 링크·메모를 수정할 수 있게 했다. 종목코드는
이 보드의 등록 식별자로 유지하고 편집 폼에서는 바꾸지 않는다. 링크 입력은
저장 전 `http`/`https`를 확인하며, 저장된 텍스트는 DOM 텍스트 노드로
표시한다. 변경은 브라우저 초안에만 적용되고 엔진 설정은 바뀌지 않는다.
브라우저 저장 실패 시 성공 안내가 오류 안내를 덮지 않도록 했다.

검증: `node --check web/board/board.js` 통과. 브라우저 상호작용과 모바일
렌더링은 실제 브라우저 도구 부재로 확인하지 않았다.

## 종목 초안 보관·복원

관심/보관 목록 필터와 카드별 보관·관심 복원 동작을 추가했다. 보관은
브라우저 초안의 표시 분류만 바꾸며 항목을 삭제하거나 엔진 설정·보유 보호를
변경하지 않는다. 이전 저장 형식에서 보관 필드가 없는 종목은 계속 관심
목록에 표시한다. 관심·보관 수를 함께 보여 준다.

검증: `node --check web/board/board.js` 통과. 실제 브라우저·모바일 동작은
브라우저 실행 도구가 없어 확인하지 않았다.

## 종목 등록 정보 보강

종목 등록 폼에 사용자가 직접 입력하는 종목명, 선택형 참고 링크와 메모를
추가했다. 카드에는 코드·이름·메모가 표시되고, 링크는 `http`/`https`만
클릭 가능하게 렌더링한다. 사용자 제공 문자열은 기존처럼 DOM
`textContent`로 출력하며 로컬 브라우저 초안으로만 보관한다. 종목 등록은
감시나 주문을 시작하지 않는다.

`node --check web/board/board.js` 통과. 실제 브라우저·모바일 렌더링은
브라우저 실행 도구가 없어 확인하지 않았다.

- 기준: GitHub `main`의 D01 계약과 `docs/NEXT-DEVELOPMENT-PLAN.md`
- 상태: **화면 골격 부분 구현**. 엔진 적용, 계정 저장, 실제 시세·주문은 연결하지 않았다.

## 구현

`web/board/`에 종목코드 6자리 수동 등록, 고정 가격/확인된 원가 대비 하락률
손절 패턴 초안 생성, 종목과 패턴 연결 화면을 만들었다. 브라우저 로컬 저장소에만
초안을 저장한다. 동일 종목 중복 등록, 잘못된 가격과 0 이상 1 미만 범위 밖의
하락률을 거절한다. 카드의 `미적용`, 상단의 `엔진 미연결`, 하단 설명은
등록만으로 감시나 주문이 시작되지 않음을 보여 준다. 사용자 입력은 DOM
`textContent`로 출력한다. 모바일 폭에서 두 칼럼을 한 칼럼으로 접는 CSS를
추가했다.

## 현재 검증과 미완료 — 2026-09-25

- `node --test web/board/board.test.js`: **8 passed**. 패턴 버전과 종목 연결,
  주문 방식 검증, JSON/CSV 형식·전체 검증·중복 무변경, JSON 내보내기 필드와
  다운로드 자원 해제를 모의 DOM/순수 파서 검사로 확인했다.
- `node --check web/board/board.js`, `node --check web/board/board.test.js`,
  `git diff --check`: 통과.
- 설치 경로에서 Chrome을 찾아 headless 실제 브라우저로 확인했다. 390×844 모바일
  emulation에서 문서 폭390px·단일 358px 열, 1365px 데스크톱 emulation에서
  두 개 570px 열을 확인했다. 모바일 emulation에서 종목 등록·패턴 연결·v2 생성
  및 LIMIT 선택·v1 비활성화 후 기존 연결 보존과 신규 연결 차단도 실행했다.
  Q12 결정 전의 매수 패턴 명세 대기 패널도 양 viewport에서 표시됨을 확인했다.
  물리 휴대폰 확인은 하지 않았다.
- 회귀 검사: Python 3.10.8 32비트 전체 pytest **159 passed**, Ruff 검사·포맷,
  가상 데모와 `pip check` 통과. 사용자 설치 Python을 썼으며 격리 `.venv`는 없다.
- 실제 기기에서의 모바일 시각 확인은 미실행이다. headless Chrome emulation은
  실제 브라우저 동작 확인이며 물리 휴대폰의 글꼴·safe area·터치 확인은 아니다.
- 종목 JSON/CSV 초안 일괄 가져오기를 구현했다. canonical 입력 형식과 검증은
  위의 2026-09-25 기록을 따르며 기존 종목 등록·저장 상태는 중복이나 오류가
  있으면 바뀌지 않는다.
- 매수 패턴의 조건·주문·시간·재진입 명세(Q12)가 없어 해당 폼은 만들지 않았다.
  대신 결정 대기 패널을 표시한다. 값을 임의로 가정하지 않는다. 지정가 가격·호가·거래 세션·미체결 처리(Q10),
  서버 저장·인증·버전 충돌·적용 승인·엔진 동기화는 미완료다.
- 실제/모의 키움 주문 API, 운영 DB 변경, 배포는 수행하지 않았다.

D02의 남은 완료 조건은 Q12의 매수 패턴 명세를 반영한 화면과 지정 모바일
브라우저에서의 실제 확인이다. 서버 명령 인증과 desired/accepted/applied 연결은
D01/D08 선행 계약·인증 기반이 필요하다. 로컬 초안은 엔진에 전달하지 않는다.
