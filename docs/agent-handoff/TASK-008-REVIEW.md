CHANGES_REQUESTED

Evidence:

- `git diff main` and `git status --short --untracked-files=all` were reviewed. The working tree contains the intended `main_live.py`/`engine.py` changes and untracked risk tests, but also a much broader 87-path change set (`core/*`, `infra/sqlite_store.py`, `opencode*`, scripts, prior handoff logs, and multiple tests). This does not match TASK-008's strict allowed-file scope; the pre-existing-change explanation does not make the complete working-tree change safe to accept as one deliverable.
- The narrow risk tests pass (`10 passed`) and the current full suite passes (`14 passed`). The tests use fake brokers/temp SQLite and show zero place/cancel calls on the tested binding-failure path. No broker transport, `.env`, or live-enable change appears in the `git diff main` file list.
- Required recovery-window coverage is absent: there is no `tests/test_main_live_recovery_window.py` (or equivalent). Therefore 14:39/14:40/14:41, 15:19/15:20/15:21, 15:34, exact 15:35/15:36, and heartbeat shutdown ordering are not proven. The test report explicitly confirms this gap.
- `on_heartbeat()` calls `engine.manage_pending_orders()` before `auto_shutdown()`. That method can reach risk cancel/retry/place paths, so at `AUTO_SHUTDOWN_HHMM` and later the implementation can start order activity before the shutdown guard runs, contradicting the spec's no-new-place/cancel invariant. This needs a direct boundary test and a fail-closed ordering/guard fix.
- `_submit_risk_sell()` calls `place_order()` before broker-ID binding. On binding failure it marks the durable event manual and returns, but does not roll back/remove the newly registered local order or clear `risk_order_events`/`sell_in_progress`; the resulting unresolved local order is not an explicit, fully fail-closed identity state. This path is not covered by the supplied tests.
- `sync_pending_orders()` calls `_mark_open_risk_manual()` globally. A malformed/unmatched pending item or pending-query exception can therefore disable every open risk event, rather than isolating the affected identity. The required independent missing/ambiguous/exception cases are not present.
- The implementation/test reports conflict: the implementation report says the full suite timed out, while the test report claims `14 passed`. The run performed during review confirms `14 passed`, but the report should be corrected and the missing acceptance tests still need to be added.

Required before PASS: constrain/identify the TASK-008 change set, add the specified recovery-window and restart/exception/identity tests, ensure the shutdown boundary cannot initiate place/cancel work, and make all broker-ID binding failure paths leave explicit, consistent local/mapping/event state without broad unrelated risk-event disabling.
